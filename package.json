{
  "name": "eisen",
  "private": true,
  "description": "Tooling for Conventional Commits validation (commitlint).",
  "license": "MIT",
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0"
  },
  "scripts": {
    "commitlint": "commitlint --from=HEAD~1 --to=HEAD"
  }
}

