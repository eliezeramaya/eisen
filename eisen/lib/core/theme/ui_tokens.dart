// Shared UI tokens for spacing, radii, and timings.

const double kRadius = 20.0;
const double kElevation = 8.0;

const Duration kAnimFast = Duration(milliseconds: 150);
const Duration kAnimNormal = Duration(milliseconds: 300);

